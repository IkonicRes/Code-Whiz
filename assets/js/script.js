


//Callback Function to allow user to start the code quiz

    //Start Timer

    //Present the user with a question

    //Evaluate whether question was answered correctly, if so advance to the next question.
        //If no more questions exist, break timer loop early

    //If not, subtract time from timer


//When timer is broken, check the time left, and if it's more than 0, assign the time left to a temporary score

//Prompt user to enter up to 4 initials or enter anonymously. If something is entered, save that string over the default "Sudo" name

//Save name and score to local storage score array

//Reset all variables to default





//Make event handler for "onclick" and call the above callback function